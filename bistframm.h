//funzioni per la gestione di un albero binario di ricerca
#ifndef BISTFRAMM_H
#define BISTFRAMM_H

typedef struct bit_node BNode;

//cerca nell'albero binario di ricerca se esiste un nodo con chiave item, se si restituisce ...
int cerca(Bnode radice, Item i, &Bnode);


#endif